export const SETTINGS =
{
  websocket: {
    reconnect_interval: 10, // [s]
    reconnect_attempts: 5
  },
  endpoints: {
    app: {
      login: "login",
      register: "register",
    },
    api: {
      endpoints: "/api/endpoints"
    }
  }
};